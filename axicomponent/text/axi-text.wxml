<text wx:if="{{text}}"
  selectable="{{selectable}}" 
  space="{{space}}"
  class="axi-text">
  {{text}}
</text>

<rich-text wx:else
  nodes="{{html}}"
  space="{{space}}">
</rich-text>

  
